<p id="notice"><%= notice %></p>

<div class="jumbotron">
  <h1 class="display-4">Projects</h1>
  <p class="lead">Hi, I'm Lindsey and this is my portfolio.</p>
  <hr class="my-4">
  <p>In it you will find things that interest me and various projects that I have done.</p>
  <p class="lead">
  
  </p>
</div>

    



<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Description</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @projects.each do |project| %>
      <tr>
        <td><%= project.title %></td>
        <td><%= project.description %></td>
        <td><%= link_to 'Show', project %></td>
        <td><%= link_to 'Edit', edit_project_path(project) %></td>
        <td><%= link_to 'Destroy', project, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
<p>
 
<%= link_to 'New Project', new_project_path %>
